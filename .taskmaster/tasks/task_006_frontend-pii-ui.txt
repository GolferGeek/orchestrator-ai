# Task ID: 6
# Title: Develop PIIPatternEditor Modal
# Status: pending
# Dependencies: 5
# Priority: high
# Description: Create modal for adding/editing PII patterns with live regex validation and preview.
# Details:
Use Ionic Modal. Form fields: name (unique), regex (live validation), data type, description, priority, category. Implement client-side regex validation and error feedback. Live preview using sample text. Integrate with API for POST/PUT.

# Test Strategy:
Unit test form validation, regex preview, and API integration. Test error feedback and rollback.

# Subtasks:
## 1. Design and Implement Ionic Modal Structure [pending]
### Dependencies: None
### Description: Set up the PIIPatternEditor modal using Ionic Modal components, ensuring proper layout and accessibility for all required form fields.
### Details:
Use IonModal and related Ionic components to create a modal dialog. Include header, content, and footer sections. Ensure modal can be triggered for both add and edit modes.

## 2. Build Form with Validation and Unique Name Enforcement [pending]
### Dependencies: 6.1
### Description: Develop the form inside the modal with fields: name (unique), regex, data type, description, priority, and category. Implement client-side validation for all fields, including uniqueness check for name.
### Details:
Use reactive forms or form state management. Validate required fields, enforce unique name (against existing patterns), and provide real-time error feedback for invalid input.

## 3. Implement Live Regex Validation and Error Feedback [pending]
### Dependencies: 6.2
### Description: Add live validation for the regex field, providing immediate feedback for invalid patterns and displaying error messages as needed.
### Details:
Parse and validate regex input on change. Catch and display syntax errors or unsafe patterns. Disable form submission if regex is invalid.

## 4. Develop Live Preview with Sample Text [pending]
### Dependencies: 6.3
### Description: Create a live preview area that applies the entered regex to a sample text, highlighting matches in real time.
### Details:
Allow user to input or select sample text. Apply regex and visually highlight matched substrings. Update preview as regex or sample text changes.

## 5. Integrate API for Add/Edit Operations [pending]
### Dependencies: 6.4
### Description: Connect the modal form to backend API endpoints for creating and updating PII patterns, handling POST and PUT requests as appropriate.
### Details:
On form submission, send data to API. Handle success and error responses, including rollback on failure. Close modal and refresh parent data on success.

