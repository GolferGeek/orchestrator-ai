fastapi
PyYAML>=6.0.1 # Explicitly pin PyYAML to a version known to build/install
uvicorn[standard]>=0.20.0 # Pin to a newer version to avoid metadata issues
python-dotenv

# Pydantic v2 for CrewAI and pydantic-settings
pydantic>=2.4.2,<3.0.0
pydantic-settings

pytest
httpx
openai
pytest-asyncio

# Add new dependencies below
httpx-sse

# New dependencies
aiofiles

# Remove crewai dependency
# crewai>=0.30.0,<0.120.0

# Additional dependencies
tiktoken
requests
psycopg2-binary
sqlalchemy
alembic
asyncpg
sse-starlette 